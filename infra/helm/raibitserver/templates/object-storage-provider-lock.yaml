apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "raibitserver.fullname" . }}-object-storage-provider-lock
  labels:
    {{- include "raibitserver.labels" . | nindent 4 }}
data:
  object-storage-provider-lock.json: |
{{ .Files.Get "files/object-storage-provider-lock.json" | indent 4 }}

